---
import { Font, Image } from 'astro:assets';
import '../styles/global.css';
import siteLogo from '../assets/site-logo.png';

interface Props {
  title?: string;
  description?: string;
  og_image?: string;
  og_url?: string;
  og_type?: string;
}

const {
  title,
  description = 'Find upcoming Winnebago Ekko owner events, get registration details, and connect with the community.',
  og_image,
  og_url = Astro.url.href,
  og_type = 'website',
} = Astro.props;

const og_title = !title ? 'Camp EKKO' : /camp ekko/i.test(title) ? title : `${title} — Camp EKKO`;

const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width" />
	<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
	<link rel="icon" href="/favicon.ico" />
	<link rel="sitemap" href="/sitemap-index.xml" />
	<link rel="canonical" href={og_url} />
	<meta name="generator" content={Astro.generator} />
  <meta name="description" content={description} />
  <title>{og_title}</title>
  <!-- Open Graph -->
  <meta property="og:type" content={og_type} />
  <meta property="og:site_name" content="Camp EKKO" />
  <meta property="og:title" content={og_title} />
  <meta property="og:description" content={description} />
  <meta property="og:url" content={og_url} />
  {og_image && <meta property="og:image" content={og_image} />}
  <!-- Twitter / X -->
  <meta name="twitter:card" content={og_image ? 'summary_large_image' : 'summary'} />
  <meta name="twitter:title" content={og_title} />
  <meta name="twitter:description" content={description} />
  <Font cssVariable="--font-display" />
  <Font cssVariable="--font-ui" />
</head>
<body>

  <header class="site-header">
    <div class="container">
      <a href="/" class="site-logo">
        <Image src={siteLogo} alt="Camp EKKO" class="logo-img" loading="eager"/>
      </a>
      <nav class="site-nav">
        <a href="/" class={`nav-link ${currentPath === '/' ? 'nav-link--active' : ''}`}>Events</a>
        <a href="/archive/" class={`nav-link ${currentPath === '/archive/' ? 'nav-link--active' : ''}`}>Archive</a>
        <a href="/about/" class={`nav-link ${currentPath === '/about/' ? 'nav-link--active' : ''}`}>About</a>
      </nav>
    </div>
  </header>

  <main>
    <slot />
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>&copy; 2026 Camp EKKO</p>
    </div>
  </footer>

</body>
</html>
